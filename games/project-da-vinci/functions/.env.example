# Cloud Functions Environment Variables

# Gemini API Key (https://ai.google.dev/ 에서 발급)
# Cloud Functions에서 Gemini Vision API를 호출할 때 사용
GEMINI_API_KEY=your-gemini-api-key-here

# Firebase Project ID
# Firebase Admin SDK 초기화 시 사용 (선택사항, 자동 감지 가능)
PROJECT_ID=your-project-id

# 로컬 개발 시 주의사항:
# 1. 이 파일을 복사하여 .env 파일 생성: cp .env.example .env
# 2. .env 파일은 .gitignore에 포함되어 있으므로 커밋되지 않음
# 3. 프로덕션 배포 시: firebase functions:config:set gemini.api_key="실제키"
